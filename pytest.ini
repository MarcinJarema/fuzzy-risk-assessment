[pytest]
# Wyciszenie DeprecationWarning pochodzącego z wnętrza biblioteki scikit-fuzzy
# (np.maximum/np.minimum z 3 argumentami) — nie dotyczy kodu projektu, a zaśmieca
# wynik testów.
filterwarnings =
    ignore::DeprecationWarning:skfuzzy.*
